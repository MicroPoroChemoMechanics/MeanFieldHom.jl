# References

All bibliographic references used across the `MeanFieldHom.jl`
documentation are collected here.  Individual chapters cite entries by
their BibTeX key via the `[key](@cite)` syntax — this page renders the
full list.

```@bibliography
```
